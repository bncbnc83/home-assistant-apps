# Security

Do not commit credentials, databases, backups, private URLs, internal hostnames,
IP addresses, certificates, or Home Assistant `options.json` files.

Deployment-specific values belong in Home Assistant app configuration.
