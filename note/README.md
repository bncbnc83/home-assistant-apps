# Trilium Notes

Home Assistant wrapper for the official pinned TriliumNext image.

The timezone is configured in Home Assistant. The repository contains no
instance-specific values or note data. Persistent data is stored in Home
Assistant `addon_config`.
