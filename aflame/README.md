# Flame Dashboard 2

Home Assistant wrapper for the official pinned Flame image.

The password is never stored in Git and must be configured in Home Assistant.
The timezone is configured in Home Assistant. Persistent data is stored in Home
Assistant `addon_config`.
